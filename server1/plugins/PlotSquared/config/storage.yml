prefix: ''

# SQLite section
sqlite:
  # Should SQLite be used?
  use: true
  # The file to use
  db: "storage"

# MySQL section
mysql:
  # Should MySQL be used?
  use: true
  host: "localhost"
  port: "3306"
  user: "molean"
  password: "123asd"
  database: "server1"
  # Set additional properties: https://goo.gl/wngtN8
  properties: 
  - "useSSL=false"
